name: auto_sos_admin
description: Dashboard administrateur Auto-SOS — Oyop MT
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.27.1
  firebase_auth: ^4.17.9
  cloud_firestore: ^4.15.9

  # State & Navigation
  provider: ^6.1.2
  go_router: ^13.2.1

  # UI
  fl_chart: ^0.68.0
  data_table_2: ^2.5.12
  intl: ^0.19.0
  timeago: ^3.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
